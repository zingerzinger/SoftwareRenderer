рендерим спец. полигоны - z буфер не важен
скайбокс выпуклый (очевидно)

берем спец полигон, клипуем в clip координатах, как любой другой
берем каждый фэйс скайбокса (который расположен вокруг камеры - вращение применить, смещение - нет (нужно подумать, как,
w нельзя оставлять нулем, оно понадобится для проекции)) // - убираю смещение из матрицы камеры, возвращаю потом
отсекаем бэкфейсы скайбокса (нормали фэйсов направлены внутрь, куда же еще)
клипуем об спец полигон (он становится выпуклым многоугольником в w-координатах)
остаток рендерим, в качестве значения для z-буфера используем z,
получившееся в результате проекции спец треугольника на экран
готово

// пришлось клипать квады скайбокса об экран стандартным клиппером, еще баги в дебаге с float`ами
      
